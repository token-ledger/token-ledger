dependencies {
    api project(':token-ledger-core')
    implementation 'org.springframework.boot:spring-boot-starter-data-redis'
}

test {
    useJUnitPlatform()

    // Redis Integration Test 제외
    exclude '**/*Redis*'
}